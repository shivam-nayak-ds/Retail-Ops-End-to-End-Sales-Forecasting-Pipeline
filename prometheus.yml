global:
  scrape_interval: 15s # How often to scrape targets by default.

scrape_configs:
  - job_name: "retail-forecasting-api"
    static_configs:
      - targets: ["host.docker.internal:8000"] # host.docker.internal allows docker containers to access host ports
